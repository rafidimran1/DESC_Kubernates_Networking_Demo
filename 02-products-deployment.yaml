apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-app
  namespace: service-ingress-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: products-app
  template:
    metadata:
      labels:
        app: products-app
    spec:
      containers:
      - name: products-app
        image: hashicorp/http-echo:0.2.3
        args:
        - "-text=Welcome to the Products Service"
        - "-listen=:8080"
        ports:
        - containerPort: 8080
