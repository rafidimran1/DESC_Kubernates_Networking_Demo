apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: service-ingress-demo
spec:
  type: ClusterIP
  selector:
    app: orders-app
  ports:
  - name: http
    port: 8080
    targetPort: 8080
